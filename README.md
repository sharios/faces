# Face Builder

A small installable **PWA** for building a cartoon face by picking alternatives
for four facial features: **hair, eyes, nose, mouth**. Mix them, hit **Random**,
and **Download** the result as a transparent‑friendly PNG.

- No build step, no framework — plain HTML/CSS/JS.
- Works offline once loaded (service worker caches the app shell + all art).
- Installable to the home screen / desktop.

## Run locally

Any static file server works; the service worker just needs an `http(s)` origin:

```bash
python3 -m http.server 8000
# open http://localhost:8000
```

## Deploy (GitHub Pages)

`.github/workflows/pages.yml` publishes the repo root on every push to `main`.
Enable it once: **Settings → Pages → Build and deployment → Source: GitHub Actions**.
The app then lives at `https://<user>.github.io/faces/`.

All paths in the app are relative, so it also works from a subfolder or from
`file://`‑style hosts that serve an origin.

## Assets

| Feature | Location | Format | Notes |
|---|---|---|---|
| Eyes | `assets/eyes/eyes-*.png` | PNG, transparent | Provided; normalised to a common aspect ratio |
| Hair | `assets/hair/*.svg` | SVG, black strokes on transparent | Sample art |
| Nose | `assets/nose/*.svg` | SVG, black strokes on transparent | Sample art |
| Mouth | `assets/mouth/*.svg` | SVG, black strokes on transparent | Sample art |
| Head base | `assets/base/head.svg` | SVG | Always drawn behind the features |

The sample hair/nose/mouth art is generated by `scripts/gen_assets.py`
(pure line work: `#141414` strokes, no fill, transparent background).
Icons come from `scripts/gen_icons.py`.

### Adding or changing options

Everything the app draws is declared in **`assets/config.json`**:

- `features.<name>.options[]` — the list of `{ id, label, src }` choices in the picker.
- `features.<name>.box` — where that layer is placed on the 1000×1000 face stage,
  as `{ x, y, w, h }` in **percent**. The image is fit into that box with
  `object-fit: contain`.
- `layerOrder` — back‑to‑front draw order.

To add a new mouth, drop `assets/mouth/my-mouth.svg` in place and add an entry to
`features.mouth.options`. Bump the cache name in `sw.js` (`facebuilder-v1` → `-v2`)
so clients pick up the new asset.

## Project layout

```
index.html            app shell
css/styles.css
js/app.js              picker + compositing + PNG export
sw.js                  offline cache
manifest.webmanifest
assets/config.json     feature + placement definitions
assets/…               art
scripts/gen_assets.py  regenerate sample SVG art
scripts/gen_icons.py   regenerate PWA icons
```
